class userchat {
  constructor(id, name, desc, images, idus) {
    this.id = id;
    this.name = name;
    this.desc = desc;
    this.images = images;
    this.idus = idus;
  }
}

export default userchat;
